# Backlog — artem

Журнал крупных изменений, вносимых в рамках работы над проектом на ветке `artem`. Каждая запись — отдельная сессия/пачка изменений, от новой сверху нет — пишем вниз по хронологии.

---

## 2026-09-13 — Инициализация Nuxt-фронта

Создан фронтенд-проект (`Nuxt 4`) — изначально в `web/` в корне репозитория, позже перенесён в `src/web/` (правильное расположение).

- Стек: Nuxt 4, Vue 3, Pinia (`@pinia/nuxt`), VueUse (`@vueuse/nuxt`). Новые зависимости добавлялись только на этом, самом первом шаге — дальше действует правило не трогать `package.json` без необходимости и согласования.
- `nuxt.config.ts`: `runtimeConfig.public.apiBase` / `wsBase` — адреса api-gateway (`:8000`) и ws-gateway (`:4333`), настраиваются через `.env` (`NUXT_PUBLIC_API_BASE`, `NUXT_PUBLIC_WS_BASE`).
- Каркас:
  - `app/stores/auth.ts` — Pinia-стор с токенами и пользователем.
  - `app/stores/chat.ts` — заготовка под чаты/сообщения.
  - `app/composables/useApi.ts` — обёртка над `$fetch` с подстановкой `Authorization: Bearer` и разлогином+редиректом на `/login` при `401`.
  - `app/composables/useWs.ts` — подключение к ws-gateway.
  - `app/types/auth.ts`, `app/types/chat.ts` — базовые типы.
  - `app/layouts/default.vue`, `app/pages/index.vue` (тестовая страница с проверкой `/health`), `app/pages/login.vue` (первая, черновая версия формы).

---

## 2026-09-17 — Изучение auth-эндпоинтов бэкенда

Прочитан код `identity` и `api-gateway` (без изменений — только исследование, согласно правилу «бэкенд только изучаем»). Ключевые выводы, на которых строится вся auth-логика фронта:

- `POST /auth/register` — `{ username (3-32), password (8-256) }` → `201 { id }`; `409` если username занят.
- `POST /auth/login` — `{ username, password }` → `{ access_token, refresh_token, token_type }`; `401 Invalid credentials`.
- `POST /auth/refresh` — `{ refresh_token }` → новый `access_token`, тот же `refresh_token` (ротации нет).
- `GET /me` (Bearer) → `{ id, username, email, display_name, created_at }` — `email`/`display_name` сейчас всегда `null` (registration их не принимает).
- Токен шлётся как `Authorization: Bearer <access_token>`; gateway сам валидирует JWT (HS256), внутрь сервисов пробрасывает `X-User-Id`.
- Logout-эндпоинта и смены пароля нет — логаут только на клиенте (сброс токенов из стора).
- Только `GET`/`POST` проксируются gateway.

---

## 2026-09-17 — Дизайн-система и страница логина/регистрации

По макету («Вход» — тёмная синяя палитра, Unbounded/Space Grotesk/IBM Plex Mono, острые углы, жёсткие тени) сделано:

- **`app/assets/styles/`** — новая папка с токенами:
  - `colors.css` — все цвета из макета (`--color-ground`, `--color-lift`, `--color-accent`, `--color-error` и т.д.).
  - `fonts.css` — `--font-display` (Unbounded), `--font-heading` (Space Grotesk), `--font-mono` (IBM Plex Mono), `--radius: 0`, `--shadow-hard`.
  - `base.css` — глобальный reset/фон (заменил старый `app/assets/css/main.css`, который удалён).
  - Шрифты подключены через `<link>` на Google Fonts в `nuxt.config.ts` (`app.head.link`) — без новых npm-пакетов.
- **`app/layouts/auth.vue`** — отдельный layout без общей шапки, для страницы логина (полноэкранный фон с сеточным паттерном).
- **`app/pages/login.vue`** — полностью переписана под макет: логотип «Телемакс», слоган, карточка с вкладками «Войти / Регистрация», моно-лейблы капсом, амбер-кнопка.
- **Механизм авторизации**:
  - `app/composables/useAuth.ts` — `login()` и `register()` бьют в реальные `/auth/login` / `/auth/register`; после регистрации (она не возвращает токены) автоматически логинит и подтягивает `/me`.
  - `app/types/auth.ts` — обновлён под реальные поля бэкенда (`access_token`, `refresh_token`, `display_name` и т.п.).
  - `app/utils/apiError.ts` — разбор ошибок FastAPI (`detail` строкой или массивом 422-ошибок) для читаемых сообщений под полями формы.
- `app/layouts/default.vue`, `app/pages/index.vue` обновлены под новые цветовые токены (чтобы не остались битые ссылки на старые CSS-переменные).

---

## 2026-09-17 — Docker для фронта

- **`src/web/Dockerfile`** — двухэтапная сборка: `node:22-alpine` builder (`npm ci` + `npm run build`) → лёгкий рантайм-образ только с `.output`, запуск `node .output/server/index.mjs` на порту 3000.
- **`src/web/.dockerignore`** — исключает `node_modules`, `.nuxt`, `.output`, `.env` и т.д.
- **`deploy/docker-compose.yml`** — добавлен сервис `web` (билд из `../src/web`, порт `3000:3000`, `NUXT_PUBLIC_API_BASE`/`NUXT_PUBLIC_WS_BASE` из env хоста, зависит от `api-gateway`/`ws-gateway`).
  - Также исправлена проблема: файл содержал смешанные CRLF/LF окончания строк, из-за чего редактор ругался на `DUPLICATE_KEY` — файл приведён к единому CRLF.

---

## 2026-09-17 — Тестовое окружение: `.env` и `docker-compose-local.yml`

Настроены `.env`-файлы для локального теста (везде используются одни и те же тестовые креды, JWT_SECRET синхронизирован между identity/api-gateway/ws-gateway — иначе валидация токена молча ломается):

- `deploy/.env`, `src/services/identity/.env`, `src/services/communication/.env`, `src/services/api-gateway/.env`, `src/services/ws-gateway/.env`, `src/web/.env`.

`deploy/docker-compose-local.yml` (гитигнорится по паттерну `docker-compose*.yml`, локальный оверрайд для конкретного хоста) — подобраны свободные внешние порты, т.к. на хосте уже заняты `5000`, `8000`, `5432`:

- `api-gateway`: `8080:8000` (было `5000:8000` — конфликт)
- `db`: добавлен `5433:5432` (для удобного подключения GUI-клиентом; `5432` занят локальным postgres)
- `ws-gateway`: `4333:4333` — без изменений
- `web`: `3000:3000` — без изменений, `NUXT_PUBLIC_API_BASE` указывает на `http://localhost:8080`

---

## 2026-09-17 — Разовое исключение: CORS в api-gateway

Обнаружено, что форма логина/регистрации не работает: браузер шлёт preflight `OPTIONS /auth/register`, gateway отвечает `405` (роут заявлен только для `GET`/`POST`, `CORSMiddleware` отсутствовал вовсе) — запрос обрывается ещё до отправки `POST`. Это блокирует любую работу фронта с API, а обойти CORS-preflight с фронта невозможно (ограничение браузера).

**По явному разовому разрешению пользователя** внесена **только эта одна правка** в бэкенд:
- `src/services/api-gateway/app/main.py` — добавлен `CORSMiddleware` (`allow_origins=["*"]`, `allow_methods=["*"]`, `allow_headers=["*"]`, без `allow_credentials`, т.к. авторизация через Bearer-токен, а не cookies).

Больше никаких изменений в `src/services/*` не вносилось и не планируется без отдельного согласования.

---

## 2026-09-17 — Роутинг после авторизации, шестерёнка настроек

- **`app/middleware/auth.global.ts`** — глобальный редирект: неавторизованный пользователь на любой странице → `/login`; авторизованный на `/login` → `/`. Сессия пока не персистентна (стор in-memory, сбрасывается по F5) — как только добавим сохранение сессии (localStorage/refresh-flow), поведение «сразу открывается главная» заработает само собой, без правок middleware.
- **`app/pages/index.vue`** — упрощена до чистого полотна (фон `--color-ground`), весь тестовый контент (проверка `/health`, конфиг, статус авторизации) убран.
- **`app/layouts/default.vue`** — в шапку справа добавлена иконка-шестерёнка (SVG), ссылка на `/settings`, подсветка акцентным цветом при наведении.
- **`app/pages/settings.vue`** — новая пустая страница настроек, тот же стиль полотна, что и на главной.
- **`app/stores/settings.ts`** + **`app/types/settings.ts`** — заготовка индивидуальных настроек пользователя: `byUserId: Record<string, UserSettings>`, геттер `current` берёт настройки текущего юзера (`authStore.user.id`) с дефолтами. Пока один плейсхолдер-параметр `theme`; будет расширяться по мере проектирования UI настроек.

---

## 2026-09-18 — Синхронизация с `dev`

`origin/dev` подтянула страницу личных чатов от `ksenia` (компоненты `chat/*`, `stores/chat.ts`, `index.vue` как экран чатов). Смержено в `artem` без конфликтов; наши файлы (шестерёнка настроек, `middleware/auth.global.ts`, `stores/settings.ts`) не задеты — `index.vue` теперь чужая страница чатов с `layout: false`, это ожидаемо.

---

## 2026-09-18 — API для настроек (второе разовое исключение на бэкенд)

По итогам обсуждения с командой (см. переписку с бэкенд-разработчиком max) решили не заводить отдельный сервис настроек, а разложить их по сервисам-владельцам сущностей: глобальные настройки — в `identity`, настройки чата — в `communication`, как отдельные независимые модули (не смешивая с `users/`/`chats/`). **По явному разрешению пользователя** реализовано полностью (4 коммита, каждый — по конвенции типов из корневого `README.md`):

- **`feat: add user settings endpoint to identity`** — новый модуль `src/services/identity/app/settings/` (`models.py`, `schemas.py`, `repository.py`, `service.py`, `router.py`), таблица `user_settings` (1:1 с `users` по `user_id`, поля `notifications_enabled`, `accept_calls`). Эндпоинты `GET/PATCH /me/settings`, авторизация через тот же `X-User-Id`, что и `/me`. Миграция `deac91b0f3e7_add_user_settings_table.py`.
- **`feat: add chat settings module to communication`** — аналогичный модуль `src/services/communication/app/settings/`, отдельная таблица `chat_settings` (composite PK `chat_id + user_id`, поле `notifications_muted`) — намеренно **не** колонка в `ChatMember`, чтобы `chats/` остался только про чаты/сообщения. Единственная точка связи двух модулей — вызов `chats_repository.is_user_in_chat()` из `settings/service.py` для проверки членства (403, если чат чужой). Эндпоинты `GET/PATCH /chats/{chat_id}/settings`. Миграция `d5d6506f04e2_add_chat_settings_table.py`.
- **`feat: route /me/settings and PATCH through api-gateway`** — в `api-gateway/app/router.py` два фикса: `resolve_target` теперь маршрутизирует по сегменту `"me"` (а не точному совпадению `path == "me"`), иначе `/me/settings` не находил сервис; в проксирующий роут добавлен метод `PATCH` (был только `GET`/`POST` — иначе `PATCH` ловил бы тот же `405`, что раньше ловил `OPTIONS` до CORS-фикса).
- **`docs: document settings endpoints in identity and communication README`** — обе таблицы HTTP API дополнены новыми строками.

**Проверено end-to-end** через `docker-compose-local.yml` (docker compose up, реальные HTTP-запросы через gateway): регистрация → логин → `GET/PATCH /me/settings` (дефолт создаётся при первом обращении, патч сохраняется) → создание чата между двумя пользователями → `GET/PATCH /chats/{id}/settings` → проверка 403 на чужом чате. Всё отработало корректно.

Заодно поправил свой локальный `docker-compose-local.yml` (не коммитится, gitignore): у `api-gateway` порты нужно объявлять как `ports: !override`, иначе compose **склеивает** список портов с базовым файлом и остаётся конфликтующий `8000:8000` — та же проблема, о которой писала `ksenia` в своём бэклоге.

---

## 2026-09-18 — Модалка настроек и подключение к API

- **`feat: open settings as a URL-driven modal over the chats screen`** — экран чатов вынесен из `pages/index.vue` в `components/ChatsScreen.vue`, рендерится напрямую в `app.vue` пока пользователь авторизован (не через `<NuxtPage>`), поэтому переход на `/settings` его не размонтирует. `index.vue`/`settings.vue` стали пустыми точками роутинга. Новый `components/SettingsModal.vue` — оверлей 80vw×90vh по центру, крестик закрытия, закрытие кликом по подложке, лёгкое затемнение фона, своя шапка «НАСТРОЙКИ» в стиле сайдбара чатов. Удалён неиспользуемый `layouts/default.vue` (дублировал шестерёнку, которая теперь только в `ChatSidebar.vue`).
- **`feat: wire settings modal to GET/PATCH /me/settings`** — реальное содержимое модалки: карточка профиля (username + дата регистрации из `/me`), два тумблера (`notifications_enabled`, `accept_calls`) через новый `composables/useSettings.ts`, кнопка «Выйти» (чисто клиентский логаут). `stores/settings.ts` упрощён до одного снапшота текущего юзера (раньше был `byUserId`-словарь-заглушка с несуществующим полем `theme`).
- Проверено вживую через Playwright (реальный Chrome, т.к. `chromium-cli`/автозагрузка браузера playwright недоступны в этом окружении — сеть режет скачивание) на полном стеке в докере: регистрация → тумблер → закрытие/переоткрытие модалки → значение осталось (сохранилось на сервере) → логаут.
- PR с этой пачкой (плюс API настроек) был смёржен в `dev` (PR #10 «Страница настроек»). После пуша CI (`ruff check .`) уронил `lint` на трёх мелочах в новых модулях `settings/` (два `__init__.py` без докстринга, одна строка >88 символов) — поправлено отдельным `style`-коммитом и предложено PR-ссылкой на `dev` (`gh` CLI недоступен в этой среде, PR открыт вручную по сгенерированной ссылке сравнения).

---

## 2026-09-18 — Поиск пользователей

Второй фронтендер не мог начинать работу без эндпоинта поиска людей. По той же логике, что и с настройками (см. выше) — не отдельный сервис, а новый маршрут в `identity/app/users/` (данные и так целиком в таблице `users`, отдельный сервис только добавил бы сетевой хоп или дублирование данных).

- **`feat: add user directory search endpoint to identity`** — `POST /users/search`:
  - `tag` — только префикс по `username`, ведущий `@` обрезается, регистр не учитывается;
  - `email`/`phone`/`name` — подстрока в соответствующем поле, регистронезависимо;
  - `query` — тот же поиск подстроки, но сразу по email/phone/name (не по тегу);
  - несколько заданных полей комбинируются через AND; если не задано ни одного — `422`;
  - `limit` — обязательный параметр без дефолта (по требованию пользователя: без зашитого топ-30, пролистывание через увеличение `limit` на каждый следующий запрос — 30, потом 60, потом 90...), сортировка по `username` для стабильной пагинации.
  - Ответ — `UserResponse` (та же схема, что у `/me`/`/users`) без пароля, но **с `id`** — без него результат поиска нельзя было бы использовать (например, для `POST /chats/direct`).
- В модель `User` добавлено поле `phone` (миграция `82ac3e19d573_add_phone_to_users.py`). `email`/`phone`/`display_name` пока всегда `NULL` — эндпоинта их установки ещё нет, так что реально сейчас работает только поиск по тегу; остальное подключится само, как только появится редактирование профиля.
- Тесты (`tests/test_search.py`, 12 штук) заводят пользователей напрямую через новую фикстуру `db_session_maker` в `conftest.py` (обходя HTTP, раз API для email/phone/name ещё нет) — покрыты префикс/подстрока/регистронезависимость/AND-комбинация/лимит-сортировка/422 на пустой фильтр и на отсутствующий `limit`/исключение пароля из ответа. Прогнано локально на одноразовом Postgres в докере (та же конфигурация, что в CI) — 26/26 зелёных, `ruff check .` чист.

---

## 2026-09-19 — Плавное открытие/закрытие модалки настроек

- **`feat: animate settings modal open/close`** — `SettingsModal` обёрнут в `<Transition>` в `app.vue`. Подложка затухает по opacity, панель дополнительно делает лёгкий scale (0.96→1) со сдвигом вверх на 8px — читается как «материализация». Открытие — 0.18с ease-out, закрытие чуть быстрее — 0.14с. Проверено вживую через Playwright (промежуточный кадр перехода зафиксирован скриншотом на 60мс после клика — панель ещё полупрозрачна/смещена).

---

## 2026-09-19 — Разбор архитектурных вопросов по настройкам

Без изменений кода — обсуждение с пользователем по итогам ревью со стороны бэкенд-разработчика:

- **`accept_calls`** в `identity` — признано архитектурной несостыковкой: сервиса звонков не существует физически, а по принципу «настройка живёт там же, где живёт фича» (тот же, что развёл mute чата по `communication`, а не `identity`) это поле должно появиться в будущем calls-сервисе, а не сейчас в `identity`. Решение по удалению — на паузе, ждём подтверждения.
- **`notifications_enabled`** — проверено по всему репозиторию (`grep`): поле нигде не читается ни в `communication`, ни в `ws-gateway`, только хранится. В отличие от `accept_calls` это **не архитектурная ошибка** (место в `identity` правильное — это глобальная настройка юзера, не привязанная к фиче), а недописанная связь: `ws-gateway` при рассылке `chat.message.created` должен бы проверять этот флаг перед отправкой конкретному пользователю, но не проверяет. Зафиксировано как известный TODO для бэкенда.

---

## 2026-09-19 — Синхронизация с `dev` и разбор PR-ревью

`origin/dev` подтянула работу ksenia «Подключение чатов к API и поиск пользователей» (PR #13) — прямой потребитель `POST /users/search`: контракт (`{ tag, limit }` → `UserResponse[]`) совпал один в один без правок с моей стороны. Слияние fast-forward, конфликтов нет, сборка зелёная.

Прошёлся по комментариям ревью (`Skuyno`) на PR #12/#13 через публичный GitHub REST API (`gh` CLI в этой среде недоступен) и точечно ответил каждому правкой:

- **`refactor: rename generic get/update repository functions in settings modules`** — `get`/`update` в `communication/app/settings/repository.py` → `get_chat_settings`/`update_chat_settings`; тот же паттерн поправлен и в `identity/app/settings/repository.py` (`update` → `update_user_settings`) для консистентности, хотя комментарий был только про `communication`.
- **`refactor: separate fetch-or-create from the read path in settings services`** — в обоих `settings/service.py` `update_settings` вызывал `get_or_create_settings` (функцию GET-эндпоинта), что читалось как ошибочное переиспользование read-логики для записи. Вынес общий приватный `_fetch_or_create` (+ `_require_membership` для чатов) — оба паблик-метода вызывают его явно; необходимость самого upsert-перед-patch осталась (иначе `PATCH` нечего патчить), просто теперь это не выглядит как вызов «чужого» метода.
- **`test: use /auth/register instead of direct DB access where possible`** — по комментарию «зачем обходить БД, если есть эндпоинт регистрации»: 6 из 12 тестов в `test_search.py`, которым нужен только `username`, переведены на `client.post("/auth/register", ...)`; `db_session_maker` оставлен только там, где реально нет эндпоинта под `email`/`phone`/`display_name`. Заодно поймал баг в своих же тестовых данных: `username="a4"` короче `min_length=3` — поправил на `a11`/`a12`/`a13`/`a14`.
- **`fix: preserve repeated query params when proxying through api-gateway`** — тот самый баг из комментария ksenia в `useChats.ts` («api-gateway теряет повторяющиеся query-параметры»). Причина: `request.query_params` (Starlette `QueryParams`) передавался в `httpx` как `params=` напрямую; `httpx` трактует его как обычный `Mapping` и вызывает `.items()`, а тот у Starlette схлопывает дубли ключей до последнего значения. Фикс — `request.query_params.multi_items()`, сохраняет все пары. Проверено на живом стеке: `GET /users?ids=A&ids=B` через gateway теперь возвращает обоих, а не только последнего. Воркэраунд ksenia (по одному запросу на `id` в `useChats.ts`) теперь избыточен, но не трогал её файл без отдельного запроса.

Все правки прогнаны через `ruff check .` (чисто) и полный прогон тестов `identity`/`communication` (26 + 12, оба зелёные) на одноразовом Postgres в докере — конфигурация как в CI.

---

## 2026-09-19 — Nginx как единая точка входа (замена CORS-костыля)

`feat: add nginx reverse proxy as single entry point for the app` — новый сервис `src/services/nginx/` (`nginx.conf`, `Dockerfile`, `README.md`), решает саму причину, из-за которой раньше пришлось городить `CORSMiddleware(allow_origins=["*"])` в `api-gateway`: теперь фронт и API живут под одним origin, и браузер вообще не считает эти запросы кросс-доменными.

- **Маршрутизация**: `/` → `web:3000`, `/api/` → `api-gateway:8000/` (префикс `/api` срезается), `/ws` → `ws-gateway:4333` (с `Upgrade`/`Connection` для WebSocket).
- **Хосты апстримов — через переменные + `resolver 127.0.0.11`**, а не литералами в `proxy_pass`: иначе nginx резолвит имена контейнеров один раз при старте конфига и **падает целиком**, если хоть один сервис (у нас — вечно нестабильный `ws-gateway`, та самая гонка со стримом NATS) ещё не поднялся.
- Две нетривиальные ловушки nginx, на которые сразу же напоролся и поправил:
  1. Переменная в `proxy_pass` отключает автоматическую подстановку пути — заданный после хоста путь идёт **буквально**, без склейки с остатком URI. Пришлось резать префикс `/api` через `rewrite ^/api/(.*)$ /$1 break;` перед `proxy_pass`.
  2. `rewrite ... break` останавливает **все** директивы rewrite-модуля, оставшиеся в блоке — включая `set`. Пришлось переставить `set $api_upstream ...` **перед** `rewrite`, иначе `$api_upstream` оставалась неинициализированной и nginx падал с `500`.
- **`deploy/docker-compose.yml`**: `web` теперь получает `NUXT_PUBLIC_API_BASE=/api`/`NUXT_PUBLIC_WS_BASE=/ws` по умолчанию (относительные пути — работают только за этим nginx); добавлен сервис `nginx`, порт `80`. Локально порт `80` занят чем-то на хосте — в `docker-compose-local.yml` (не коммитится) проброшен на `8081`, и оттуда же убрано переопределение `NUXT_PUBLIC_API_BASE`/`WS_BASE` у `web` (наследует относительные пути из базового файла).
- **`src/web/app/composables/useWs.ts`** — `WebSocket` не умеет резолвить относительные урлы сам, поэтому добавлена `resolveWsBase()`: если `wsBase` начинается с `/`, достраивает `ws://`/`wss://` от `window.location` (схема зависит от `location.protocol`); абсолютные адреса (прямой запуск без nginx) проходят как есть, без изменений в поведении.

**Проверено end-to-end**: полный стек за nginx (порт `8081` локально) — `register`/`login`/`/me`/`PATCH /me/settings` через `/api/...` отработали корректно; настоящая браузерная сессия (Playwright, реальный Chrome) через nginx зафиксировала **0 OPTIONS/preflight-запросов** за весь сценарий регистрация→чаты→настройки — прямое доказательство, что CORS для основного пути пользователя больше не участвует вообще. `CORSMiddleware` в `api-gateway` не убирал — остаётся как фолбэк для прямых обращений в обход nginx (мобильное приложение в будущем, тестовые скрипты и т.п.).

---

## 2026-09-19 — CORS-мидлварь остаётся, ws-gateway больше не падает на старте

**Вопрос про удаление `CORSMiddleware` из `api-gateway`** — обсудили и решили **не убирать**. Nginx закрывает CORS только для сценария «весь стек в докере через nginx»; повседневная разработка фронта (`npm run dev` в `src/web/` без докера, как описано в `BACKLOG-ksenia.md`) по-прежнему бьёт в `api-gateway` напрямую с другого origin — там CORS всё ещё нужен по-настоящему. Уберём, только если решим, что прямой доступ к `api-gateway` из браузера в обход nginx больше никому не нужен.

**`fix: retry NATS subscribe instead of crashing ws-gateway on startup`** — наконец исправлен старый известный баг: `ws-gateway` подписывается на JetStream-стрим `CHATS`, который создаёт `communication` при своём старте; если `ws-gateway` стартовал раньше — ловил `no stream matches subject` и падал через `log.Fatalf`, роняя весь контейнер из-за обычной гонки при холодном старте, а не реальной поломки.

- `internal/broker/nats.go` — новый `Client.SubscribeToMessagesWithRetry` (15 попыток по 2с, запас 30с), логирует каждую неудачную попытку вместо мгновенного падения.
- `cmd/main.go` — вызывает ретрай-версию вместо одноразовой `SubscribeToMessages`.
- `deploy/docker-compose.yml` — `ws-gateway` теперь ждёт `communication: condition: service_healthy` в `depends_on` (раньше зависел только от `redis`/`nats`) — убирает гонку на уровне оркестрации в обычном случае, ретрай остаётся страховкой на то, что `depends_on` не покрывает (ручной рестарт контейнера и т.п.).

**Проверено принудительным воспроизведением гонки**: остановил `communication`, полностью пересоздал `nats` (чтобы стрима `CHATS` точно не было), перезапустил `ws-gateway` напрямую — увидел в логах попытки 1-9 с `no stream matches subject` **без падения контейнера**, затем поднял `communication` — на 9-й попытке подписка прошла успешно, `ws-gateway` продолжил слушать `:4333`.

---

## 2026-09-19 — Разбор: готов ли второй фронтендер к realtime-сообщениям

Без изменений кода (кроме README) — второй фронтендер обещал за день подключить чат к WebSocket, чтобы сообщения приходили без перезагрузки страницы. Проверил по коду (не по README — оно оказалось неточным), чего ему для этого хватает и чего нет:

- **Готово**: `useWs.ts` уже шлёт `?token=<accessToken>` ровно так, как ждёт `GET /ws?token=<JWT>`; резолвинг `wsBase` уже работает и напрямую, и через nginx (см. запись про nginx выше); `chatStore.addMessage()` уже дедуплицирует по `id`, так что WS-эхо на своё же отправленное через REST сообщение не задвоится.
- **Не хватает — но это чисто фронт**: `connect()`/`disconnect()` нигде не вызываются, `onmessage` вообще не назначен — сокет открывается и ничего не делает. Работы на сегодня хватает без единой правки бэкенда.
- **Ловушка**: в `useWs.ts` есть метод `send()`, который выглядит как «отправка сообщения через сокет» — бэкенд такие сообщения полностью игнорирует (проверил `ReadPump` в `client.go` — там даже нет обработчика, не просто «игнорирует», а физически некому обработать). Если довериться этому методу — ничего не заработает.
- **Главная находка** — `docs: fix ws-gateway README to match the actual outbound message envelope`: README сервиса утверждал, что клиенту улетает «исходный JSON события» из NATS. Неправда — `internal/ws/hub.go` заворачивает событие в `{ type: "message.created", data: {...} }`, причём поле id называется **`message_id`**, а не `id` (как в REST). Если бы второй фронтендер ориентировался на README, а не на код — сломал бы дедупликацию по `id` и потратил время на отладку несуществующей проблемы. README поправлен, теперь описывает реальный конверт.

---

## 2026-09-19 — Порт `web` больше не публикуется; диагностика «не работает логин на 8081»

После введения nginx `web` продолжал публиковать свой порт `3000:3000` — по привычке можно было открыть приложение напрямую там, но относительные `NUXT_PUBLIC_API_BASE=/api`/`NUXT_PUBLIC_WS_BASE=/ws` резолвятся от текущего origin, а на `:3000` `/api` обслуживать некому. Запрос попадал в собственный роутер Nuxt-приложения, ловил `302` от нашего же `middleware/auth.global.ts` (как на любой несуществующий путь для неавторизованного пользователя) — снаружи выглядело как молчаливо проваленный логин/регистрация без внятной ошибки.

**`build: stop publishing web's own port now that nginx is the entry point`** — убрал `ports: "3000:3000"` у `web` в `docker-compose.yml` (и в локальном оверрайде). Теперь у `web` вообще нет прямого входа с хоста — единственный путь внутрь всего стека — через `nginx`.

Отдельно проверил жалобу «и на `8081` тоже не работает» — пересобрал `web`+`nginx` с нуля и прогнал реальный браузерный сценарий (Playwright, настоящий Chrome) через `localhost:8081`: регистрация → логин → `/me` → `/chats` — всё `200`/`201`, редирект на экран чатов. На тот момент решил, что проблема не воспроизвелась (закэшированное состояние). **Ошибся** — пользователь прислал скриншот консоли браузера: запросы реально уходили на абсолютный `http://localhost:8000/auth/login` вместо `/api/auth/login`, и это било в CORS (порт `8000` на хосте не проброшен локально — только `8080`).

Настоящая причина: `docker exec web env` показал, что внутри контейнера `NUXT_PUBLIC_API_BASE` вообще не задана. Когда я убирал `ports: "3000:3000"` у `web` в локальном оверрайде, заодно (ещё до этого) убрал оттуда и `environment:`-блок, положившись на то, что он подхватится из базового `docker-compose.yml` при объединении файлов. Это работает, только если оба файла запускать **вместе** (`-f docker-compose.yml -f docker-compose-local.yml`); если когда-либо (кем угодно) локальный файл поднимается отдельно — `environment` для `web` там просто отсутствует, и Nuxt тихо откатывается на захардкоженный дефолт `http://localhost:8000` из `nuxt.config.ts`. Внешне это выглядит как «не работает логин» без единой явной причины в UI.

Исправил — вернул явный `environment: NUXT_PUBLIC_API_BASE: /api / NUXT_PUBLIC_WS_BASE: /ws` в `docker-compose-local.yml` (дублирует базовый дефолт, но теперь оверрайд самодостаточен). Перепроверил тем же браузерным сценарием — `apiBase`/`wsBase` в `window.__NUXT__.config` теперь корректно `/api`/`/ws`, полный флоу регистрация→логин→`/me`→`/chats` снова зелёный. Предупреждение об этой ловушке добавил в `src/services/nginx/README.md`, чтобы не наступить снова с другим оверрайдом.

---

## 2026-09-20 — Разбор масштабирования + healthcheck для NATS

**Обсуждение масштабирования** (без изменений кода) — коллега прислал доводы про stateless-архитектуру, WebSocket, presence, БД как бутылочное горлышко. Сверил с реальным кодом репозитория:

- `identity`/`communication`/`api-gateway` — уже полностью stateless (никакого in-memory состояния между запросами), плюс JWT вместо серверных сессий — даже сильнее требования «сессии в Redis».
- `ws-gateway` — WebSocket-шина между инстансами уже фактически реализована через NATS: `c.js.Subscribe(subject, handler)` без durable-имени/queue-группы → каждый инстанс получает копию каждого события и сам решает, доставлять ли локально подключённому юзеру. Ровно паттерн «шина + локальная фильтрация», который предлагал коллега — просто на NATS, а не на отдельном Redis Pub/Sub.
- Presence — уже в Redis (`internal/presence/redis.go`), не в памяти инстанса.
- Единственный реально непокрытый пункт — БД: один Postgres без реплик/шардирования, будет первым бутылочным горлышком при росте до сотни сервисов. Плюс SPOF на уровне инфры (один NATS, один Redis, один Postgres без кластеризации) — отдельная ось от «stateless-код».

**`feat: добавить healthcheck для NATS и зависимость communication от него`** — по комментарию коллеги к ревью `nats.go`. `nats` теперь стартует с `-m 8222` (monitoring-порт) и healthcheck на `/healthz`; `communication` ждёт `nats: condition: service_healthy` в `depends_on` вместо неявного предположения, что NATS уже готов к моменту своего собственного старта (communication создаёт JetStream-стрим `CHATS` в `lifespan`, до этого — race).

**Попутно найден и исправлен реальный баг в `docker-compose-local.yml`** (гитигнорится, но чуть не привёл к тихой поломке): файл дублировал **целые блоки** сервисов, скопированные из базового `docker-compose.yml`, вместо того чтобы переопределять только нужные ключи (порты, env). Поскольку `command`/`depends_on` при слиянии compose-файлов **полностью заменяются**, а не сливаются по вложенным ключам — устаревшие копии в оверрайде тихо съедали свежие изменения из базового файла (новый `-m 8222` у `nats`, новую зависимость `communication`→`nats`, ранее добавленную `ws-gateway`→`communication`). Переписал `docker-compose-local.yml` на минимальные, точечные оверрайды (только `ports`/`environment`, без копирования всего сервиса) — теперь слияние с базовым файлом действительно подтягивает актуальные `command`/`depends_on`, а не версию на момент копирования. Проверено холодным стартом всего стека — `nats` доходит до `healthy` раньше `communication`, полный флоу регистрации через nginx зелёный.

---

## 2026-09-20 — Шесть новых фич: профиль, правка/удаление сообщений, поиск, «печатает», непрочитанные

По плану (см. обсуждение выше в этот же день): всё, что реализуемо без внешней инфраструктуры (без S3, без SMTP), сделано полностью за один заход — 4 коммита, три сервиса (`identity`, `communication`, `ws-gateway`) плюс точечный фикс `api-gateway`.

**Аватар и сброс пароля — по факту:**
- Аватар: **только задел**, как договорились. `avatar_url` — новая колонка в `users`, всегда `null`, возвращается в `UserResponse`. Загрузка файлов (S3/SeaweedFS) — отдельная будущая задача, само поле в контракте уже есть, чтобы фронт не переделывал типы повторно.
- Сброс пароля: **без email/SMTP**, как и просили — `POST /me/password` меняет пароль по текущий+новый для уже авторизованного пользователя. Требует текущий пароль (иначе украденный access-токен, живущий всего 15 минут, позволил бы захватить аккаунт одной сменой пароля). Бампает `token_version` — все refresh-токены, выданные раньше, отзываются. Это первое реальное использование `token_version` с тех пор, как его завели.

**`identity`**: `PATCH /me` — `display_name`/`email` (обе колонки существовали, но были недостижимы; email проверяется на базовую форму и уникальность, `409` при конфликте).

**`communication`**:
- `PATCH`/`DELETE /chats/{id}/messages/{message_id}` — редактирование и удаление своих сообщений. Удаление — **soft delete** (`is_deleted` + очистка `body`), не физическое: не ломает пагинацию по `before_msg_id`.
- `GET /chats/{id}/messages/search` — поиск по подстроке (`ILIKE`, регистронезависимо), `limit` обязателен без дефолта — тот же принцип, что и в поиске пользователей у `identity`.
- `POST /chats/{id}/typing` — эфемерный сигнал «печатает», ничего не пишет в БД. Обходит однонаправленность WebSocket (клиент физически не может слать через сам сокет — `ReadPump` ничего не обрабатывает) через уже существующий мост REST → NATS → `ws-gateway`.
- `POST /chats/{id}/read` + новая таблица `chat_read_states` — отметка прочитанного, `GET /chats` теперь отдаёт `unread_count` на каждый чат (один SQL-запрос с коррелированным подзапросом на все чаты сразу, не N+1).
- Новые NATS-subjects: `chat.message.updated`, `chat.message.deleted`, `chat.message.read`, `chat.message.typing` — все укладываются в уже существующий wildcard стрима `CHATS` (`chat.message.>`), схему JetStream трогать не пришлось.

**`ws-gateway`**: раньше подписка была жёстко на `chat.message.created` с одним типом исходящего конверта. Теперь подписка на `chat.message.*` (wildcard), обработчик получил subject вместе с payload'ом, и `buildOutboundEvent` (чистая функция, покрыта юнит-тестами `hub_test.go`) разбирает событие в конверт под конкретный случай — без этого рефакторинга новые события просто не долетали бы до клиента, даже дойдя до NATS.

**`api-gateway`**: доп. фикс — проксирующий роут пропускал только `GET/POST/PATCH`, новый `DELETE` ловил бы `405` прямо на гейтвее. Та же история, что раньше была с `PATCH` до появления настроек — видимо, стоит взять за привычку сразу добавлять метод в `api-gateway` в том же PR, где заводится новый REST-метод где угодно ниже по цепочке.

**Тесты**: 5 новых в `identity` (31 всего), 13 новых в `communication` (25 всего), 6 юнит-тестов в `ws-gateway` (`go test ./...`, было 0). Всё прогнано локально на одноразовом Postgres — конфигурация как в CI, всё зелёное. Плюс live-прогон полного сценария через `docker compose` + nginx (регистрация → смена пароля → чат → правка/поиск/typing/read/delete сообщения) — все статусы ожидаемые.

**README** всех трёх сервисов обновлены под новые эндпоинты. Заодно поправил в README `communication` ещё одно устаревшее утверждение («WS Gateway передаёт JSON как есть») — то же семейство ошибки, что и с README `ws-gateway` неделей ранее: документация не поспевала за кодом.

---

## Рекомендации для фронтенда (ветка `ksenia`) по новым эндпоинтам

Всё ниже — то, что реально нужно знать при интеграции, не общий список эндпоинтов (он есть в README сервисов).

### Профиль

- `PATCH /me` — тело `{ display_name?, email? }`, оба поля опциональны, можно слать только одно. `409` — email уже занят другим аккаунтом. `422` — email не проходит базовую проверку формы (`x@y.z`).
- `avatar_url` в `UserResponse` — **всегда `null`** сейчас, эндпоинта загрузки нет. Можно уже сейчас завести в UI место под аватар (fallback на инициалы, как сейчас), но саму загрузку показывать рано.
- `POST /me/password` — тело `{ current_password, new_password }`, ответ `204` без тела. `401` — текущий пароль не совпал. **После успешной смены пароля все refresh-токены (в том числе, возможно, на других устройствах/вкладках) становятся недействительны** — текущая сессия с уже полученным access-токеном продолжит работать ещё до 15 минут, но при следующем `/auth/refresh` словит `401` и придётся заново логиниться. Стоит либо сразу разлогинивать текущую сессию тоже (попросить ввести новый пароль), либо явно предупреждать пользователя.

### Сообщения: правка, удаление, поиск

- `PATCH /chats/{id}/messages/{message_id}` — тело `{ body }`, `403` если не свой чат **или** не свой message, `404` если сообщения нет. Успешный ответ — полный `MessageResponse` с проставленным `edited_at`.
- `DELETE /chats/{id}/messages/{message_id}` — `204`, тело сообщения после этого приходит **пустой строкой** (`body: ""`), а не `null` — проверяйте `is_deleted`, не `body`, чтобы показать «сообщение удалено».
- `GET /chats/{id}/messages/search?query=...&limit=...` — **`limit` обязателен**, без него `422` (тот же принцип, что и в поиске людей: сами решаете размер страницы, сервер не подставляет дефолт).
- Все три ручки бьют `403`, если вызывающий не состоит в чате — обрабатывайте так же, как уже обрабатываете `403` на `/chats/{id}/messages`.

### «Печатает…» и прочитанные — через WebSocket, не через поллинг

`useWs.ts` (`onmessage` пока не подключён — это первое, что вам предстоит доделать, я писал об этом ранее) получит новые типы конвертов:

```json
{ "type": "typing", "data": { "chat_id": "...", "user_id": "..." } }
{ "type": "message.read", "data": { "chat_id": "...", "user_id": "...", "last_read_message_id": "..." } }
{ "type": "message.updated", "data": { "message_id": "...", "chat_id": "...", "sender_id": "...", "body": "...", "edited_at": "...", "created_at": "..." } }
{ "type": "message.deleted", "data": { "message_id": "...", "chat_id": "...", "sender_id": "..." } }
```

Поле id сообщения везде **`message_id`**, не `id` — как и в `message.created`, который уже разбираете.

- Отправка «печатает»: `POST /chats/{id}/typing` (без тела, `204`) на каждый инпут-событие — рекомендую дебаунсить на фронте (например, не чаще раза в 2-3 секунды), сервер не троттлит сам.
- Отметить прочитанным: `POST /chats/{id}/read` с `{ last_read_message_id }` — вызывайте, когда пользователь открыл чат/долистал до конца, а не на каждое сообщение.
- `unread_count` в `GET /chats` — считает только чужие сообщения после вашего последнего `read`. Обновляется сразу после `POST .../read`, дополнительно перезапрашивать `/chats` для этого не нужно — можно оптимистично занулить счётчик на клиенте.

### Общее

- Метод `send()` в `useWs.ts` по-прежнему мёртвый код — бэкенд игнорирует любые сообщения от клиента по сокету. Для «печатает» используйте REST, не пытайтесь слать через сокет.
- Если что-то не работает как в этой памятке — сверяйтесь с кодом сервисов (`app/chats/router.py`, `internal/ws/hub.go`), а не только с README: у нас уже дважды бывало, что README отставало от реального поведения.

---

## 2026-09-20 — Медиа-вложения: file-orchestrator, SeaweedFS, вложения в сообщениях

По явному разрешению пользователя (разовое, на конкретный набор правок) добавлены два новых сервиса и доработан `communication`/`api-gateway`/`ws-gateway` для поддержки файлов в чатах.

### api-gateway — потоковое проксирование

Раньше `router.py` полностью буферизовал тело запроса (`await request.body()`) и ответа в памяти — несовместимо с загрузкой/отдачей больших файлов. Переписан на потоковую передачу: `request.stream()` как тело исходящего запроса, `StreamingResponse` поверх `upstream.aiter_bytes()` для ответа. Это касается **всех** проксируемых путей, не только файловых.

### file-orchestrator (новый сервис)

Хранит метаданные вложений (таблица `files`: `id, chat_id, uploader_id, original_filename, content_type, size_bytes, storage_key, status, created_at, updated_at, deleted_at`) — источник правды, сами байты лежат в SeaweedFS.

- **Загрузка**: `POST /files?chat_id=...` с сырым телом (без multipart), метаданные — в заголовках `X-Filename`/`Content-Type`. Стримится напрямую в SeaweedFS filer (`PUT /attachments/{file_id}`). Раз в мегабайт (настраивается) публикует `file.upload.progress` в NATS; по завершении — `file.upload.completed`.
- **Скачивание**: `GET /files/{id}` — file-orchestrator сам стримит байты из SeaweedFS обратно клиенту (осознанно выбран полный контроль над доступом, а не signed URL на сам SeaweedFS).
- **Лимиты размера**: два уровня, действует более строгий — админский `max_upload_size_bytes` (по умолчанию `None` = не ограничено, задел под будущую админку) и физическое свободное место на диске SeaweedFS (`GET /status` на volume-сервере). Проверяется и по заявленному `Content-Length`, и по факту полученных байт по ходу стрима.
- **Удаление**: подписка на `chat.message.deleted` (тот же NATS-subject, что слушает и публикует `communication`) — при удалении сообщения file-orchestrator удаляет блобы вложений и помечает файлы `deleted`.
- **Авторизация**: и загрузка, и скачивание проверяют членство в чате через новый internal-эндпоинт `communication`: `GET /internal/chats/{chat_id}/members/{user_id}`. Для рассылки прогресса загрузки по WebSocket нужен список всех участников чата — добавлен `GET /internal/chats/{chat_id}/members` (без user_id), возвращает все `user_ids`.
- **Валидация вложений**: `POST /internal/files/validate` — принимает `chat_id` + список `file_ids`, возвращает только те, что реально существуют, готовы (`status=ready`) и принадлежат этому чату. Вызывается из `communication` при отправке сообщения.

### communication — вложения к сообщениям

- Новая таблица `message_attachments` (`message_id`, `file_id` — составной PK, `file_id` **не** внешний ключ: указывает на строку в чужой БД file-orchestrator, тот же подход, что и у `sender_id`/`created_by`).
- `SendMessageRequest`/`MessageResponse` получили `attachment_file_ids: list[UUID]` (можно приложить несколько файлов). Перед сохранением сообщения список валидируется через file-orchestrator (`_validate_attachments`), при невалидном id — `400`.
- Важный нюанс: `create_message()` теперь возвращает `tuple[Message, bool]` (сообщение, было ли реально создано) — иначе повторная отправка с тем же `client_msg_id` (идемпотентность) попыталась бы второй раз вставить те же строки `message_attachments` и упала бы на дублировании composite PK.
- При удалении сообщения (`delete_message`) id вложений попадают в payload события `chat.message.deleted` — их читает file-orchestrator для очистки блобов (ws-gateway это поле просто игнорирует).
- JetStream-стрим `CHATS` теперь создаётся/обновляется с `subjects=["chat.message.>", "file.upload.>"]` — раньше файловые события в него бы просто не попадали (не матчились под фильтр). И `communication`, и `file-orchestrator` идемпотентно обеспечивают этот список при подключении (add_stream, а если уже существует — update_stream).
- Заодно найден и исправлен реальный, ранее маскированный баг в `tests/conftest.py`: `async with AsyncMock() as x` не привязывает `x` к тому же моку, если явно не выставить `mock.__aenter__.return_value = mock` — раньше это молчаливо работало «случайно правильно» для одного из моков, но всплыло на новом тесте валидации вложений.

### ws-gateway — трансляция прогресса загрузки

Добавлена подписка на `file.upload.progress`/`file.upload.completed` (в дополнение к `chat.message.*`), с маппингом в WS-события `upload.progress`/`upload.completed`. Рассылка — тем же механизмом `recipient_ids`, что и для сообщений (recipient_ids теперь кладёт в payload сам file-orchestrator, получая список участников чата через internal-эндпоинт communication).

### Инфраструктура

- `deploy/docker-compose.yml`: добавлены сервисы `seaweedfs` (`chrislusf/seaweedfs`, единый combined-режим — master+volume+filer, без публичного порта) и `file-orchestrator` (build, зависит от `db`/`nats`/`seaweedfs`/`communication` healthy). `api-gateway` теперь дополнительно ждёт `file-orchestrator` healthy.
- `.github/workflows/ci.yml`: добавлена джоба `test-file-orchestrator`, по образцу `test-identity`/`test-communication`.
- Тесты: 3 новых теста для `file-orchestrator` (upload/download roundtrip, 404 на несуществующий файл, internal-валидация с учётом чата) — SeaweedFS-вызовы замоканы (in-memory блобы), НЕ реальный контейнер. 5 новых тестов для вложений в `communication` (отправка/список/невалидный id/идемпотентность/удаление публикует id вложений). Итого `communication`: 30/30, `file-orchestrator`: 3/3, всё через throwaway `postgres:17` в Docker + venv, как обычно.

### Рекомендации для фронтенда (ветка ksenia)

- **Отправка сообщения с вложением**: сначала `POST /api/files?chat_id=...` (сырое тело — файл как есть, БЕЗ multipart/FormData-обёртки; заголовки `X-Filename: <имя файла>`, `Content-Type: <mime>`) → в ответе `{ id, status: "ready", ... }`. Дальше этот `id` кладём в `attachment_file_ids` при `POST /api/chats/{id}/messages`. Можно приложить несколько файлов — просто несколько id в массиве.
- **Прогресс загрузки для самого отправителя** — не ждите WebSocket, это чисто клиентская задача: используйте `XMLHttpRequest.upload.onprogress` (у `fetch` нативного progress на upload нет) — байты уже есть у браузера, лишний круг через бэкенд не нужен.
- **Прогресс загрузки для остальных участников чата** — по WebSocket придут события `{ type: "upload.progress", data: { file_id, chat_id, uploader_id, bytes_uploaded, size_bytes } }` (size_bytes может быть `null`, если клиент не прислал `Content-Length`) и `{ type: "upload.completed", data: { file_id, chat_id, uploader_id, size_bytes } }`.
- **Скачивание/отображение вложения**: `GET /api/files/{file_id}` — обычный поток байт с `Content-Type` и `Content-Disposition: attachment`. Для картинок можно просто использовать как `src` у `<img>` (браузер сам поставит `Authorization`, если это делает ваш `useApi`/axios-интерцептор — обычный `<img src>` заголовки не проставляет, так что для превью в чате, скорее всего, потребуется либо fetch+blob URL, либо отдельный неавторизованный путь в будущем).
- Ограничение на размер файла на сервере есть, но по умолчанию не выставлено явно (кроме физического места на диске) — конкретное сообщение об ошибке `413`, текст на ваше усмотрение.

---

## 2026-09-20 — Загрузка/смена/удаление аватара пользователя (identity)

Последний пункт из списка «что реализовано для фронтенда» без бэкенда — закрыт.

- `PUT /api/me/avatar` — загрузка/замена аватара: сырое тело запроса (файл как есть, без multipart), заголовок `Content-Type` определяет MIME (разрешены только `image/jpeg`, `image/png`, `image/webp`, `image/gif`, иначе `415`). Лимит размера — `max_avatar_size_bytes` (по умолчанию 5 МБ), проверяется и по `Content-Length`, и по факту принятых байт (`413` при превышении). Возвращает обновлённый профиль с новым `avatar_url`.
- `DELETE /api/me/avatar` — удаляет блоб и очищает `avatar_url` (профиль возвращается с `avatar_url: null`, фронт возвращается к инициалам).
- `GET /api/users/{userId}/avatar` — отдаёт байты картинки (стримингом), доступно любому авторизованному пользователю (как и остальной user-directory) — не только владельцу, иначе нельзя было бы показывать чужие аватарки в чатах.
- Хранение — то же SeaweedFS, что и у чатовых вложений, но напрямую из `identity` (без file-orchestrator): ключ блоба фиксированный — `avatars/{user_id}`, поэтому повторная загрузка естественным образом заменяет старый файл тем же PUT, отдельной очистки не нужно.
- `avatar_url` в ответах теперь выглядит как `/users/{userId}/avatar?v=<random>` — случайный суффикс на каждую загрузку нужен для сброса кэша браузера/CDN, так как URL по структуре не меняется, а сама картинка — меняется.
- `avatar_url` по-прежнему нельзя выставить через `PATCH /me` — только через эти два новых эндпоинта, которые ещё и физически владеют блобом.
- 4 новых теста в `identity` (upload+serve, неверный content-type, 404 без аватара, delete). Итого `identity`: 35/35.

### Рекомендации для фронтенда (ветка ksenia)

- Смена/загрузка аватара: `PUT /api/me/avatar`, тело — сам файл (как в `fetch(url, { method: 'PUT', body: file, headers: { 'Content-Type': file.type } })`, без `FormData`). В ответе — весь профиль, `avatar_url` уже обновлён.
- Удаление аватара («убрать»): `DELETE /api/me/avatar` — тоже возвращает профиль, `avatar_url: null`, дальше на фронте показываем инициалы как сейчас.
- Показ чужого/своего аватара: `<img>`/`src` не подойдёт напрямую, т.к. нужен `Authorization`-заголовок (как и с файлами чата) — грузите через `fetch` + `URL.createObjectURL(blob)`, ключ по `avatar_url` из профиля (он уже содержит query-параметр для сброса кэша, повторный `fetch` того же пользователя после аплоада отдаст новую картинку).
- Валидация на фронте до отправки не обязательна, но лучше проверить тип/размер заранее (jpeg/png/webp/gif, до 5 МБ) — иначе `415`/`413` от сервера.

---

## 2026-09-20 — Баги первого реального запуска (docker compose up)

При первом полном подъёме стека с новыми сервисами всплыло 4 бага, все исправлены и проверены живым end-to-end прогоном (регистрация → аватар upload/download/delete → загрузка файла в чат → сообщение с вложением, всё через `nginx -> api-gateway`):

1. Healthcheck `seaweedfs` использовал `http://localhost:8888/` — в контейнере `localhost` резолвится в `::1` первым, а filer слушает только IPv4, поэтому health вечно падал по connection refused. Исправлено на `127.0.0.1`.
2. `identity` не имел `httpx` в зависимостях, хотя новый `app/users/storage.py` для аватаров его использует — контейнер падал на импорте при старте. Добавлено в `requirements.txt`/`pyproject.toml`.
3. В `deploy/initdb/01-databases.sql` не было базы для `file-orchestrator` — по умолчанию у него получался тот же `POSTGRES_DB`, что у `communication`, и alembic падал на чужой ревизии. Добавлены `telemax_file_orchestrator`/`test_telemax_file_orchestrator`. **Важно**: этот файл выполняется только при первой инициализации volume `pgdata` — на уже существующем окружении новую базу нужно создать вручную (`CREATE DATABASE telemax_file_orchestrator;`) и поправить `POSTGRES_DB` в локальном `.env` `file-orchestrator`, если он уже был создан по образцу `communication`.
4. `api-gateway` не пропускал метод `PUT` (нужен для `PUT /me/avatar`) и не форвардил заголовок `X-Filename` (нужен для `POST /files`, у него нет другого способа передать имя файла) — оба места в проксирующем роуте.

---

## 2026-09-24 — Ролевая модель: superuser / admin / user

По явному запросу пользователя. Реализовано в `identity` (единственный сервис, владеющий пользователями) + минимальная правка `api-gateway`.

### Роли

Три роли в новой колонке `users.role`: `superuser`, `admin`, `user`. Правила «кто кем может управлять» — в новом `app/roles.py` (`MANAGEABLE_ROLES: {superuser: {admin, user}, admin: {user}}`). `user` не входит ни в один набор как ключ — обычный пользователь не может управлять вообще никем.

- **superuser** — единственный аккаунт, создаётся автоматически при первом старте сервиса (`ensure_superuser_seeded`, вызывается в lifespan `app/main.py`) с логином/паролем из `SUPERUSER_USERNAME`/`SUPERUSER_PASSWORD` (по умолчанию `admin`/`admin` — обязательно сменить перед реальным деплоем). Идемпотентно: если superuser уже есть — не создаёт ещё одного. Если желаемый логин уже занят обычным аккаунтом — не паникует и не подменяет чужой аккаунт, а логирует ошибку и требует ручного вмешательства (сменить `SUPERUSER_USERNAME` или переименовать существующий аккаунт).
- **admin** — может создавать/удалять только `user`-аккаунты.
- **user** — обычный аккаунт, создаётся либо через публичную `POST /auth/register` (регулируется уже существующим `ALLOW_REGISTRATION`, всегда создаёт роль `user` — самостоятельная регистрация физически не может создать admin/superuser), либо через `POST /admin/accounts` от имени admin/superuser.

### Новые эндпоинты (`/admin/...`, все требуют роль admin/superuser)

- `POST /admin/accounts` — `{ username, password, role: "admin"|"user" }` → `201` с профилем. `403`, если роль вызывающего не может создавать запрошенную роль (admin, пытающийся создать admin/superuser); `409` при занятом логине.
- `DELETE /admin/accounts/{id}` — `204`. `400` при попытке удалить самого себя (отдельная защита, до проверки ролей); `403`, если цель — `superuser` (никто не может его удалить, включая самого superuser) или роль вызывающего не может удалять роль цели; `404`, если аккаунта нет.
- `GET /admin/accounts?limit=&offset=` — список аккаунтов для экрана управления, новые сверху.

`api-gateway`: добавлен маршрут `admin` → identity (`resolve_target`), иначе `/api/admin/...` отвечал бы `404` ещё до того, как запрос доходит до identity.

### Прочее

- `UserResponse` теперь включает `role` — фронт может скрывать/показывать ссылку на админку по этому полю в `GET /me`.
- Миграция `f4a1c6e9d2b7_add_role_to_users` — колонка `role VARCHAR(16) NOT NULL DEFAULT 'user'`, существующие пользователи автоматически становятся `user`.
- 12 новых тестов (`test_admin.py`): посев superuser + идемпотентность + коллизия логина, создание/удаление по всем комбинациям ролей, self-delete guard, доступ к списку. Заодно найден и исправлен баг в тестовой инфраструктуре: фикстура `db_session_maker` не чистила `users` между тестами (в отличие от `client`), из-за чего аккаунт `admin` из одного теста утекал в следующий и ловил `UniqueViolationError` — добавлена такая же очистка, как у `client`. Итого identity: 48/48.

### Рекомендации для фронтенда (ветка ksenia)

- `GET /me` теперь возвращает `role: "superuser"|"admin"|"user"` — по этому полю показывать/скрывать пункт «Админка» в настройках (сам `SettingsModal`/меню на усмотрение фронта, эндпоинт не был частью этой задачи).
- Управление аккаунтами: `POST /api/admin/accounts` (тело `{ username, password, role }`, `role` только `"admin"` или `"user"` — `superuser` через API создать нельзя, он ровно один и появляется сам при первом деплое) и `DELETE /api/admin/accounts/{id}`. Оба `403`, если у текущего пользователя недостаточно прав — обрабатывать как обычную ошибку доступа, специального UI-статуса не нужно.
- `GET /api/admin/accounts?limit=50&offset=0` — для таблицы/списка в будущей админ-панели, пагинация смещением.
- Так как это новые эндпоинты без готового UI — на этой сессии сделан только бэкенд; страница/раздел админки на фронте не создавались.

---

## Правила, установленные пользователем по ходу работы

- Работаем только над фронтендом (`src/web/`); бэкенд — только для изучения контрактов API, не редактируем без явного разового разрешения на конкретную правку.
- Не добавлять/обновлять npm-зависимости без необходимости и согласования.
- После крупных изменений — вести и дополнять этот файл (`BACKLOG-artem.md`).
- Перед началом работы и перед PR в `dev` — синхронизировать `artem` с `origin/dev`.
- Коммиты оформлять строго по таблице типов из корневого `README.md` (`feat`/`fix`/`docs`/`sec`/`build`/`ci`/`refactor`/`revert`/`style`/`init`) — префикс типа на английском, а вот текст описания после двоеточия — на русском.
