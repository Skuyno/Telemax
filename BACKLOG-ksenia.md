# Backlog — ksenia

Журнал крупных изменений, вносимых в рамках работы над проектом на ветке `ksenia`. Каждая запись — отдельная сессия/пачка изменений, пишем вниз по хронологии (новое снизу).

Параллельный журнал по ветке `artem` — `BACKLOG-artem.md`.

---

## 2026-09-18 — Локальное окружение

Всё ниже — локальные файлы, в гит не попадают (`.env` и `docker-compose*.yml` в `.gitignore`).

- Созданы все `.env` по образцу `.env.example`: `deploy/.env`, `src/services/{identity,communication,api-gateway,ws-gateway}/.env`, `src/web/.env`.
  - `JWT_SECRET` одинаковый в `identity`, `api-gateway`, `ws-gateway` — иначе токен молча не проходит валидацию.
  - `POSTGRES_DB`: `telemax_identity` / `telemax_communication` — ровно те базы, что создаёт `deploy/initdb/01-databases.sql`.
- `deploy/docker-compose-local.yml` — на хосте заняты `8000` и `5432`:
  - `api-gateway`: `8080:8000`, `db`: `5433:5432`; `NUXT_PUBLIC_API_BASE` в `.env` фронта → `http://localhost:8080`.
  - **Важно:** для `api-gateway` нужен `ports: !override`. Без него compose **склеивает** списки портов с базовым файлом, `8000:8000` остаётся и контейнер падает на занятом порту.
- Запуск: `docker compose -f docker-compose.yml -f docker-compose-local.yml up -d --build` из `deploy/`. Для вёрстки удобнее погасить контейнер `web` и запускать фронт через `npm run dev` в `src/web/` (hot-reload).

---

## 2026-09-18 — Страница личных чатов

По макету «Личные чаты» (та же палитра, что у «Входа»; Unbounded / Space Grotesk / IBM Plex Mono, острые углы, жёсткие тени).

- **`app/pages/index.vue`** — главная теперь = экран чатов. Заготовка-полотно перезаписана.
  - `definePageMeta({ layout: false })`: по макету общей шапки нет — логотип в шапке списка, справа своя шапка диалога. `layouts/default.vue` не тронут, на нём остаётся `/settings`.
  - Две колонки (список 300px + диалог); на `<900px` — одна колонка, в диалоге появляется кнопка «назад».
- **`app/components/chat/`** — новые компоненты:
  - `Sidebar.vue` — логотип + акцентная линия 2px, шестерёнка настроек (переехала из общей шапки), поиск, список.
  - `ListItem.vue` — строка чата: квадратный аватар с инициалами, имя, превью «Вы: …», время; hover и активное состояние (левая рамка 2px accent).
  - `Dialog.vue` — шапка с именем, лента с разделителями дат, поле ввода + кнопка «Отправить» (Enter тоже отправляет).
  - `Message.vue` — входящий/исходящий пузырь со временем.
  - `Empty.vue` — «Чатов пока нет» / «Выберите чат».
- **`app/utils/chatTime.ts`** — время в списке (`20:54` / `вчера` / `пн` / `ДД.ММ`), время в пузыре, разделители «Сегодня / Вчера / 18 сентября».
- **`app/types/chat.ts`** — типы один в один с ответами `communication`: `Chat { id, title, initials, lastMessage }`, `Message { id, chatId, senderId, body, createdAt }`.
- **`app/stores/chat.ts`** — стор пустой по умолчанию; `meId` берётся из `authStore.user.id`; `visibleChats` (локальный поиск по названию), `setChats` / `setMessages` / `sendMessage` (пока только локально). В шапке файла — комментарий с маппингом на эндпоинты для будущего подключения.

---

## 2026-09-18 — Подключение чатов к API и поиск пользователей

После того как Артём добавил `POST /users/search` в `identity`, экран чатов подключён к реальному бэкенду — переписываться можно прямо в интерфейсе. Экран теперь живёт в `components/ChatsScreen.vue` (Артём вынес его из `index.vue` под модалку настроек), изменения внесены туда.

- **`app/composables/useChats.ts`** — все запросы чатов, по образцу `useAuth`:
  - `loadChats()` — `GET /chats` → по каждому чату `GET /chats/{id}/members` → имена собеседников через `GET /users?ids=`. Чат с самим собой поддержан (единственный участник — я).
  - `loadMessages()` / `loadOlderMessages()` — история по 50 сообщений, курсор `before_msg_id`; бэкенд отдаёт новые→старые, в ленте разворачиваем.
  - `sendMessage()` — `POST /chats/{id}/messages` с `client_msg_id: crypto.randomUUID()` (идемпотентность).
  - `searchUsers()` — `POST /users/search { tag, limit: 20 }`, ведущий `@` срезается, себя из выдачи убираем, ответы на устаревшие запросы отбрасываются.
  - `openChatWith()` — `POST /chats/direct`; если чат с человеком уже есть, бэкенд возвращает его — дублей нет.
- **`app/stores/chat.ts`** — локальная «отправка» убрана; чаты сортируются по последнему сообщению (бэкенд отдаёт без сортировки), новое сообщение поднимает чат наверх; добавлены пагинация (`hasMoreByChatId`), состояния загрузки/ошибки, результаты поиска людей.
- **`components/chat/Sidebar.vue`** — поле «Поиск по @username» одновременно фильтрует свои чаты (блок «Чаты») и ищет людей на сервере (блок «Люди», запрос через 300 мс после последней буквы). Вернулась иконка ✏ «Новый чат» — ставит фокус в поиск. Состояния «Загрузка…», «Ищем…», «Никого не нашли», ошибки.
- **`components/chat/UserItem.vue`** — новая строка найденного пользователя: аватар с инициалами, имя, `@username`.
- **`components/chat/Dialog.vue`** — отправка через сервер: кнопка блокируется на время запроса, при ошибке показывается текст, черновик не стирается. Подгрузка старых сообщений при прокрутке вверх с сохранением позиции.
- **`components/chat/Empty.vue`** — вернулась кнопка «▶ Новый чат» (фокус в поиск).
- **`components/ChatsScreen.vue`** — загрузка чатов при входе (стор сбрасывается, чтобы не остались данные прошлого пользователя), загрузка истории при выборе чата.

---

## 2026-09-19 — Сообщения в реальном времени (WebSocket)

Новые сообщения приходят сразу, без перезагрузки страницы. Бэкенд (`ws-gateway`) был готов, работа только на фронте.

- **`app/composables/useWs.ts`** — обработка входящих событий (`onMessage`), автоматическое переподключение при обрыве (паузы 1с → 2с → 4с… до 15с), `onReconnect` после восстановления связи. Метод `send()` помечен комментарием: ws-gateway игнорирует сообщения от клиента, отправка только через REST.
- **`app/composables/useChats.ts`**:
  - `handleRealtimeEvent()` — разбирает `message.created`; id в событии называется **`message_id`**, а не `id`, как в REST. Своё сообщение приходит эхом и отсеивается по id. Если пишет человек, чата с которым ещё нет в списке, — список перезагружается.
  - `resync()` — после переподключения перечитывает чаты и открытую переписку, чтобы не потерять пришедшее во время обрыва.
  - Имена собеседников снова одним запросом `GET /users?ids=…&ids=…` — Артём починил потерю повторяющихся параметров в `api-gateway`, обход по одному id убран.
- **`app/types/chat.ts`** — тип события `MessageCreatedEvent`.
- **`components/ChatsScreen.vue`** — сокет подключается при входе и закрывается при выходе из аккаунта.

**Исправлен старый баг адреса сокета:** в `.env.example` и `nuxt.config.ts` с первого дня стояло `ws://localhost:4333` — без пути `/ws`. ws-gateway слушает только `/ws`, на корень отвечает `404`, поэтому соединение не открывалось и фронт молча переподключался по кругу. До подключения вебсокета ошибку никто не замечал — сокет не использовался. Через nginx (`/ws`) баг не проявлялся. Исправлено на `ws://localhost:4333/ws`.

> Кто запускает фронт через `npm run dev`: допишите `/ws` в свой `src/web/.env` — `.env` не в гите и сам не обновится.

---

## 2026-09-19 — Модалка настроек по макету «Профиль»

- **`components/SettingsModal.vue`** — две колонки: слева «НАСТРОЙКИ» с амбер-линией и пункт «Профиль», справа шапка «Профиль» с крестиком. Карточка профиля (аватар-инициалы, имя, `@username · на Телемаксе с …` из `/me`), поле «Имя пользователя» только для чтения, блок «Уведомления» с двумя переключателями, кнопки «Сохранить» и «Выйти из аккаунта». На `<900px` — на весь экран, меню сверху. Загрузку/сохранение (`useSettings`, стор) и анимацию открытия не трогала.
- **Поведение изменилось:** раньше переключатели сохранялись сразу при клике, теперь изменения копятся и уходят по «Сохранить». Кнопка неактивна без изменений, отправляются только изменённые поля, после сохранения — «Сохранено», при ошибке — текст ошибки.
- **`components/SettingsToggle.vue`** — вид переключателя по макету (крупнее, квадратный ползунок).
- **`app/utils/initials.ts`** — функция инициалов вынесена из `useChats.ts`, общая для чатов и настроек.

---

## 2026-09-20 — Редизайн экрана чатов

По новому макету, только из того, что поддерживает бэкенд.

- **`components/ChatsScreen.vue`** — сетка на фоне (как на странице входа), локальные скругления `--chat-radius` / `--chat-radius-lg` только для экрана чатов; глобальный `--radius` не трогала.
- **`components/chat/Sidebar.vue`** — кнопки «Новый чат» и настроек в рамках, скруглённый поиск, карточки чатов с отступами, подвал с моим профилем.
- **`components/chat/ListItem.vue`, `UserItem.vue`** — карточки со скруглением, аватар 44px.
- **`components/chat/Message.vue`** — аватар рядом с сообщением, свои сообщения амбером с тёмным текстом, чужие синие, время внутри пузыря.
- **`components/chat/Dialog.vue`** — разделители дат таблетками, скруглённое поле ввода, квадратная кнопка отправки со стрелкой.
- **`components/chat/Empty.vue`** — сетка на фоне.

---

## 2026-09-21 — Локальное окружение: file-orchestrator

Всё ниже — локально, в гит не попадает.

- `src/services/file-orchestrator/.env` — те же креды, `POSTGRES_DB=telemax_file_orchestrator`.
- Базы `telemax_file_orchestrator` / `test_telemax_file_orchestrator` созданы вручную (`CREATE DATABASE`) — `initdb` выполняется только при первом создании тома `pgdata`.
- Стек пересобран целиком: добавились контейнеры `file-orchestrator` и `seaweedfs`.

---

## 2026-09-21 — Профиль: отображаемое имя, почта, аватар

- **`composables/useAuth.ts`** — `updateProfile()` (`PATCH /me`), `uploadAvatar()` (`PUT /me/avatar`, файл сырым телом), `removeAvatar()` (`DELETE /me/avatar`); профиль после ответа сразу обновляется в сторе.
- **`types/auth.ts`** — `avatarUrl` / `avatar_url` в профиле.
- **`components/SettingsModal.vue`**:
  - поля «Отображаемое имя» и «Электронная почта» (рядом «Имя пользователя» только для чтения), сохраняются той же кнопкой «Сохранить», что и переключатели;
  - пустое имя или почту бэк не принимает — при очистке поля подсказка и блокировка сохранения; ошибки `409` / `422` по почте переведены на русский;
  - загрузка фото по клику на аватар (значок камеры), «Сменить фото» / «Удалить фото», проверка до отправки: JPG/PNG/WEBP/GIF до 5 МБ.
- **`components/UserAvatar.vue` + `composables/useAvatarSrc.ts`** — общий аватар: картинка скачивается с токеном (обычный `<img src>` его не передаёт) и превращается в blob-ссылку, каждая загружается один раз и берётся из кэша; нет фото — инициалы.
- Аватар подставлен в список чатов, поиск людей, шапку диалога, сообщения, подвал сайдбара, карточку в настройках.

---

## 2026-09-21 — «В сети», непрочитанные, прочтение, «печатает…»

- **`types/chat.ts`** — у чата `peerId`, `unreadCount`, `avatarUrl`; события `presence`, `typing`, `message.read`.
- **`stores/chat.ts`** — статусы «в сети» по пользователю, «печатает» с автоистечением через 4 с, последнее прочитанное собеседником сообщение, рост счётчика непрочитанных при новом сообщении в неоткрытом чате.
- **`composables/useChats.ts`**:
  - `unread_count` и собеседник чата берутся из `GET /chats` и участников;
  - `markRead()` — `POST /chats/{id}/read` при открытии чата и при новом сообщении в открытом чате, **только если окно видно и в фокусе**; при возврате в окно открытый чат дочитывается;
  - `sendTyping()` — `POST /chats/{id}/typing` не чаще раза в 2,5 с.
- **`composables/useWs.ts`** — `send()` не отправляет, пока сокет не открыт; после появления нового чата уходит `{"type":"presence.sync"}` (команда Макса в ws-gateway).
- **`components/chat/ListItem.vue`** — голубая точка «в сети», амбер-счётчик непрочитанных, «печатает…» вместо превью.
- **`components/chat/Dialog.vue`** — «в сети» / «печатает…» в шапке, «печатает» с анимированными точками над полем ввода.
- **`components/chat/Message.vue`** — у своих сообщений одна галочка (отправлено) или две (прочитано).
- **`components/ChatsScreen.vue`** — таймер для «печатает…», дочитывание при фокусе окна.

---

## 2026-09-21 — Правка, удаление, поиск сообщений и вложения

- **`types/chat.ts`** — у сообщения `editedAt`, `isDeleted`, `attachmentIds`; типы вложения и загружаемого файла; события `message.updated` / `message.deleted`.
- **`composables/useChats.ts`**:
  - `editMessage()` (`PATCH`), `deleteMessage()` (`DELETE`), `searchMessages()` (`GET …/messages/search`, `limit` обязателен), `loadUntil()` — подгрузка истории, пока не найдётся нужное сообщение;
  - `sendMessage()` принимает `attachment_file_ids`;
  - события `message.updated` / `message.deleted` обновляют ленту и превью в списке;
  - в событии `message.created` нет вложений — после чужого сообщения в открытом чате последние сообщения дозапрашиваются и вложения дописываются.
- **`stores/chat.ts`** — `patchMessage()`; если своё сообщение уже пришло по сокету (без вложений), ответ сервера дописывает вложения, а не отбрасывается как дубль.
- **`composables/useFiles.ts`** — загрузка через `XMLHttpRequest` ради прогресса (`POST /files?chat_id=`, файл сырым телом, имя в `X-Filename` через `encodeURIComponent` — кириллицу в заголовке браузер не пропускает); получение вложения с кэшем, для своих загрузок — сразу из локального файла.
- **`components/chat/Attachment.vue`** — картинка превью, по клику открывается поверх чата (закрыть — крестик, клик по фону, Esc; есть «Скачать»); остальные файлы — плашка с именем, размером и скачиванием.
- **`components/chat/Message.vue`** — вложения, пометка «изменено», «Сообщение удалено» в пунктирной рамке, кнопки ✏ и 🗑 у своих сообщений при наведении, подсветка найденного.
- **`components/chat/Dialog.vue`**:
  - правка: текст переходит в поле ввода, полоска «Редактирование», Enter — сохранить, Esc — отменить;
  - удаление с подтверждением;
  - скрепка: несколько файлов, плашки с процентом и полоской прогресса, отправка с текстом или без;
  - лупа в шапке: поиск по переписке, список совпадений, клик — прокрутка к сообщению с подсветкой.
- **`components/chat/ListItem.vue`** — у сообщения только с файлом превью «Вложение».

---

## 2026-09-22 — «Избранное» (чат с самим собой)

- Бэк уже поддерживает чат с собой: `POST /chats/direct` с собственным id в `peer_user_id` (повторный вызов возвращает тот же чат).
- **`types/chat.ts`** — у чата признак `isSaved`.
- **`composables/useChats.ts`**:
  - `openSavedChat()` — открывает «Избранное», при первом нажатии создаёт его;
  - в `loadChats()` чат, где единственный участник — я, получает название «Избранное», без аватарки и без «Вы:» в превью.
- **`stores/chat.ts`** — для «Избранного» не показываются «в сети» и «печатает…», в превью нет «Вы:».
- **`components/chat/SavedIcon.vue`** — значок закладки вместо аватарки.
- **`components/chat/Sidebar.vue`** — кнопка-закладка «Избранное» в шапке рядом с «Новый чат» и «Настройки», ошибка открытия выводится в списке.
- **`components/chat/ListItem.vue`**, **`components/chat/Dialog.vue`** — значок закладки; в шапке подпись «заметки для себя»; в пустом чате подсказка «Сохраняйте сюда заметки, ссылки и файлы — их видите только вы».
- **`components/chat/Message.vue`** — проп `hideStatus`: в «Избранном» у сообщений нет галочек.
- Вложения, правка, удаление и поиск работают так же, как в обычном чате.
- Проверено через API на локальном стеке: создание (201), повторное открытие без дубля (200), единственный участник, отправка и поиск сообщения.

---

## Правила, установленные пользователем по ходу работы

- Работаем только над фронтендом (`src/web/`); бэкенд — только для изучения контрактов API, не редактируем без явного разового разрешения.
- Не добавлять/обновлять npm-зависимости без необходимости и согласования.
- **Не вводить новые цвета** — только существующие токены из `app/assets/styles/colors.css`. Если подходящего нет — заменить на существующий, а не добавлять свой.
- **Не показывать в UI то, чего нет на бэкенде**, и не использовать выдуманные данные.
- После крупных изменений — вести и дополнять этот файл (`BACKLOG-ksenia.md`).
